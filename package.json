{
  "name": "farm-bayan",
  "version": "1.7.0",
  "description": "نظام إدارة المزرعة — بيان",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "playwright test",
    "test:permissions": "playwright test tests/permissions",
    "test:ssot": "playwright test tests/ssot",
    "test:smoke": "playwright test tests/smoke",
    "scan": "node scripts/safety-scan.js",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}