{
  "name": "farm-bayan-android",
  "version": "1.7.0",
  "description": "تطبيق بيان المزرعة — نسخة Android APK",
  "scripts": {
    "sync-web": "node ../scripts/sync-apk-www.js",
    "sync": "npm run sync-web && npx cap sync android",
    "build": "npm run sync-web && npx cap build android",
    "open": "npx cap open android",
    "run": "npm run sync-web && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}